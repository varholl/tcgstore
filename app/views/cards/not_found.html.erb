<div class="text-center py-5">
  <i class="fas fa-question-circle fa-4x text-muted mb-3"></i>
  <h1 class="mb-2"><%= t('cards.show.not_found_title') %></h1>
  <p class="text-muted mb-4"><%= t('cards.show.not_found_message') %></p>
  <%= link_to cards_path, class: "btn btn-primary" do %>
    <i class="fas fa-arrow-left me-1"></i><%= t('cards.show.back_to_cards') %>
  <% end %>
</div>
