<p><%= t('reservation_mailer.cancelled.greeting', name: @reservation.display_name) %></p>

<p><%= t('reservation_mailer.cancelled.body', id: @reservation.id) %></p>

<table class="items-table">
  <thead>
    <tr>
      <th><%= t('reservation_mailer.common.card_name') %></th>
      <th><%= t('reservation_mailer.common.edition') %></th>
      <th><%= t('reservation_mailer.common.quantity') %></th>
      <th><%= t('reservation_mailer.common.price') %></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.card.name %></td>
        <td><%= item.card.edition_name %></td>
        <td><%= item.quantity %></td>
        <td><%= number_to_currency(item.unit_price) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p><%= t('reservation_mailer.cancelled.closing') %></p>
